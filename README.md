# instagram-message-data
Python tool for converting Instagram Direct Message HTML exports into clean, readable txt files. The created txt file will only contain names, timestamps, and messages. Media clutter (reel links, descriptions, etc) are removed. 
